# 2026-09-24 · boom_bug

- **R300 收官提交**（b9c65be）：上轮遗留的 3 个文档同步文件（README / playtest-log / visual-upgrades）入库。
- **R301 PixiJS 垂直切片落地**（ef0a1fd）：09-17 引擎选型决策（垂直切片 A/B 实验）兑现。
  - `slice/` 独立实验件：sim 层零改动，PixiJS 8.21 重做案例1 表现层（真 sprite 程序化纹理、加法辉光粒子、
    大威力顿帧、trauma² 震屏、自定义 shader 冲击波=径向位移+波前色散）。
  - `slice/case1-setup.js` 逐位复刻 editor.buildEntities 语义（含 0.15+i×0.35 点燃节奏），浏览器与 Node 共用输入。
  - `slice/baseline.mjs` Node 基线：case1 seed=4103 → 爆炸6·连锁×4·击倒9·一爆多杀2·断绳0，
    **checksum=3746700737，与浏览器切片逐位一致** —— 确定性跨渲染器成立（引擎迁移最大风险解除）。
  - 无头 Chrome 实测 161fps、零 JS 错误；152 门禁全绿。
  - 工程侧小改：server.js MIME 补 `.mjs`；eslint ignores 加 `slice/`；pixi.js 进 devDependencies。
  - 踩坑：①syncBodies 初版漏挂 ticker → 运行中画面全空（统计对、画面没有）；②爆炸实测 blastRadius 50–70
    世界单位/威力 45–70，特效参数必须按实测校准，不能拍脑袋。
- **验证工具链经验**：bsk 在本沙箱内 `session start` 挂死被 SIGTERM（输出文件 0 字节），不可用；
  改用 **playwright-core（装入 ~/.workbuddy/binaries/node/workspace）+ channel:'chrome' 驱动本机系统 Chrome**，
  ESM 下 NODE_PATH 无效 → 用 createRequire 指向 workspace 绝对路径。脚本存 .shots/verify-slice.mjs（gitignored）。
- 待决策（用户）：切片观感对比后，是否走引擎迁移 / 何时启动；Backlog 其余项（UGC 后端、3D、多端）不变。
- **R301b 访问链路修复**（13:25）：用户报 /slice/ 未展示 → 实为 8123 无服务（npm start 未跑）；顺手修 server.js
  三个连环 bug：目录 URL 404 → 回退 index.html；MIME 按目录路径取 → octet-stream 触发下载；const file 赋值
  TypeError 静默 404 → let。教训：curl 验证必须看 Content-Type 头，不只是状态码；无头 Chrome 端到端是照妖镜。
  slice 页加 4s 自诊断探针（__sliceReady）。Canvas 版入口 = 根路径 /（npm start 后 localhost:8123）。
- **R302 顺滑度+质感专项**（13:55，用户反馈"效果不够好、要更流畅"）：插值渲染（60Hz 模拟 × 任意刷新率，
  prevState 快照 + alpha=acc/DT + 角度最短弧插值）；真 bloom（半分辨率 RT 480×288 + 阈值 pass 0.5 + BlurFilter
  8/2 + 加法叠回 alpha 0.55——**无阈值会把中灰背景整帧洗白**）；连锁慢镜头一局一次 0.55s×0.35；双层闪光/缓出
  冲击环/火花闪烁/纸屑碎片；DPR 锐化。确定性零漂移（checksum 3746700737 不变），164fps。经验：SwiftShader 下
  全分辨率 bloom RT 掉帧到 41 → 半分辨率解决；冲击环纹理放大后显笨要按最终显示尺寸画。
- **R303 爆炸表现逐项对齐原版**（14:45，用户反馈"没有原版好看"→ 放弃自创配方）：粒子系统改 type 驱动，
  逐项移植 ui/particles.ts 配方——线拖尾渐冷火花（g=700，三档色）、四段渐变闪光+白核收缩、金调双描边环+
  时间涟漪蓝环、焦痕（cap 24/12s）、全屏白闪（gain/decay 对齐）、余烬、贴地扬尘、三色纸屑、暖烟长大、
  相机全套参数（trauma 0.22+min(.55,p/200)、flash 3.4/s 衰减、kick 推镜、roll）、连锁浮字。
  **核心教训：特效别自创，逐项抄打磨过的原版**。新增图层：scorchC/sparkG(Graphics 每帧重画)/floatLayer/
  flashOverlay(全屏白)。checksum 仍逐位一致（36eda4e）。
- **R303b 黑罩修复 + 爆炸纯享**（15:30，fac6e28）：①bloom 阈值 shader 只归零 rgb 不归零 alpha → 编辑态正常、
  一点燃 RT 写入不透明场景就变成 55% 黑罩（用户实测抓中）；修 = rgb 与 alpha 同时乘 k。教训：**滤镜输出的
  alpha 通道也是合成的一部分，暗部必须输出全零**。②RT 480×288+scale2 实际只裁到场景左上四分之一 →
  改 resolution:0.5 的 960×576（逻辑映射正确、像素量不变）。③按用户要求移除画面浮字（连锁/一爆多杀），
  爆炸纯享，统计只留侧栏 DOM。
